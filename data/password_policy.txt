Password resets are a high-volume, low-risk IT support workflow.

A valid password reset workflow requires verifying the user identity, sending a secure reset link, and prompting the user to retry login.

If the user remains locked out after a reset, create a ticket for human IT support.
